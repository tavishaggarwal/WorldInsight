(function(){
    angular.module('WorldInsight').controller('loginController',['$window', function ($window) {
        var init = function () {
            // Scroll to top of page
            $window.scrollTo(0, 0);
        }();
        
    }]);
    
}());